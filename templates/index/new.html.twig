{%  extends "base.html.twig" %}
{%  block body  %}

    <h1>{{ pokeForFighting.name }} is your pokemon for fighting</h1>

    <h1>Pokemon's list</h1>
    <table class="table table-striped table-hover ">
    <thead>
    <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Courbe XP</th>
        <th>Evolution</th>
        <th>Type 1</th>
        <th>Type 2</th>
    </tr>
    </thead>
    <tbody>
    {% for poke in pokemons %}
        <tr>
            <td>{{ poke[0] }}</td>
            <td>{{ poke[1] }}</td>
            <td>{{ poke[2] }}</td>
            <td>{{ poke[3] }}</td>
            <td>{{ poke[4] }}</td>
            <td>{{ poke[5] }}</td>
            <td><a href="{{path("catch",{ name:poke[1], evolution:poke[2]}) }}"  class="btn btn-success">catch it!</a></td>
    {% endfor %}
    <tr>

{%  endblock %}